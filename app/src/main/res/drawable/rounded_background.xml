<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white" />
    <corners android:radius="3dp" /> <!-- Радиус закругленных углов 3dp -->
    <stroke
        android:width="1dp"
        android:color="@android:color/darker_gray" /> <!-- Опционально: можно изменить или удалить -->
</shape>
